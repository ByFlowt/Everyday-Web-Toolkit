name: Deploy to GitHub Pages

on:
    push:
        branches:
            - main

permissions:
    contents: read
    pages: write
    id-token: write

jobs:
    deploy:
        runs-on: ubuntu-latest
        environment:
            name: Site-Passwords
            url: ${{ steps.deployment.outputs.page_url }}

        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: Inject password
              env:
                USERS: ${{ secrets.USERS }}
              run: |
                echo "Before: $(grep USERS_PLACEHOLDER index.html)"
                sed -i "s|USERS_PLACEHOLDER|${USERS}|g" index.html
                echo "After: $(grep USERS_PLACEHOLDER index.html || echo 'Placeholder replaced')"

            - name: Setup Pages
              uses: actions/configure-pages@v5

            - name: Upload artifact
              uses: actions/upload-pages-artifact@v3
              with:
                  path: .

            - name: Deploy to GitHub Pages
              id: deployment
              uses: actions/deploy-pages@v4
